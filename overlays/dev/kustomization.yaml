apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: dev
namePrefix: dev-

images:
  - name: image-name
    newName: nginx
    newTag: latest

patches:
  - target:
      kind: Deployment
      name: my-sample-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1